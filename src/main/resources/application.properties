spring.application.name=workouttracker

# Persist data to a local file instead of the default in-memory database,
# so workouts survive an application restart.
spring.datasource.url=jdbc:h2:file:./data/workouttracker
spring.datasource.driver-class-name=org.h2.Driver
spring.jpa.hibernate.ddl-auto=update
